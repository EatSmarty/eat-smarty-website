import clsx from "clsx";
import React from "react";

export default function Producttitle() {
    return (
        <h1 className={clsx(
            "text-2xl",
        )}>CocaCola
        </h1>
    );
}
