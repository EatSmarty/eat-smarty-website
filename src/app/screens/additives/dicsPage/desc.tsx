import { FC } from 'react'

interface descProps {
  
}

const Desc: FC<descProps> = ({}) => {
  return <div>
    <div className='desc-show'>
        <header>
            <h1>E-Number</h1>
        </header>
    </div>
  </div>
}

export default Desc